/**
 * 
 * @param {string} message alert message
 * @param {string} mode
 */
export function customAlert(message, mode=0){
    let pallete = {
        "-1":"#ff4545",
        "0":"#a2a2a2",
        "1":"#c8b237"
    }
}

function generateAlert(title, message, color){
    let alertID = "custom-alert-asd552"
    let div = document.createElement("div")
    div.id=alertID
}